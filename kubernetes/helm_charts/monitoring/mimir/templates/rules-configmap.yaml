apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "mimir.fullname" . }}-rules
  labels:
    {{- include "mimir.labels" . | nindent 4 }}
data:
  {{- (.Files.Glob "rules/*.yml").AsConfig | nindent 2 }}
