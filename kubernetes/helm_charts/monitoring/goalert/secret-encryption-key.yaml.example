apiVersion: v1
kind: Secret
metadata:
  name: goalert-encryption-key
  namespace: monitoring
type: Opaque
stringData:
  # Not a password to remember - any random value works. Generate one with:
  #   openssl rand -hex 32
  DATA_ENCRYPTION_KEY: REPLACE_ME
